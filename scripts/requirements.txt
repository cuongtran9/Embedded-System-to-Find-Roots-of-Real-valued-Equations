# Equation Solver — Python Dependencies
# Dùng cho: scripts/run_tests.py, scripts/generate_report.py, raspi/rootpi.py

# Numerical computing (dùng trong rootpi.py)
numpy>=1.24.0

# Testing framework
pytest>=7.0.0
